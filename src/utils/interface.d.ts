export type IVector = {
  x: number;
  y: number;
};

/* 
  矩阵3阶 对应 数组类型
*/
export type Matrix3_1 = [number, number, number];
export type Matrix3_3_css = [number, number, number, number, number, number];
export type Matrix3_3 = [number, number, number, number, number, number, number, number, number];
export type Matrix4_4 = [
  number,
  number,
  number,
  number,
  number,
  number,
  number,
  number,
  number,
  number,
  number,
  number,
  number,
  number,
  number,
  number,
];
