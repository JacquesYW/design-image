import { Modal } from 'antd';
// import style from "./index.module.less";

// interface ImageSelectProps {}
const ImageSelect = (/* props: ImageSelectProps */) => {
  return <Modal open={false}>ImageSelectProps</Modal>;
};

export default ImageSelect;
